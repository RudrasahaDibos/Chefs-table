
const Recipe = ({recipe}) => {
    console.log(recipe)
      const {recipe_name ,
        image_url
        } = recipe
    return (
        <div>
            
        </div>
    );
};

export default Recipe;