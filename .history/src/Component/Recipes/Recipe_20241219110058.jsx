
const Recipe = ({recipe}) => {
    console.log(recipe)
      const {recipe_name ,image_url,ingredients,preparing_time,short_description

      } = recipe
    return (
        <div>
            
        </div>
    );
};

export default Recipe;