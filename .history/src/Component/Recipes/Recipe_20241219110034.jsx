
const Recipe = ({recipe}) => {
    console.log(recipe)
      const {recipe_name ,image_url,ingredients,} = recipe
    return (
        <div>
            
        </div>
    );
};

export default Recipe;